spring:
  application:
    name: auth
  profiles:
    active: defalut
  config:
    import: optional:configserver:http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: refresh